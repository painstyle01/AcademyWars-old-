CREATE TABLE stats(
id INTEGER UNIQUE PRIMARY KEY,
int INTEGER,
str INTEGER,
spd INTEGER,
hp INTEGER);

CREATE TABLE player(
id INTEGER UNIQUE PRIMARY KEY,
nick INTEGER UNIQUE,
nick_change INTEGER,
status INTEGER,
guild INTEGER)
